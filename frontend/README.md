# SkyPakistan Airways - Frontend

Frontend Vite + React + TypeScript app for SkyPakistan Airways.

Quick start:

```
cd frontend
npm install
cp .env.example .env
npm run dev
```

Default API URL is `http://localhost:4000/api`. Ensure backend is running.
